import { Footer, Header, RestaurantList } from '../components';

import React from 'react';
export const Home = () => {
  return (
    <div>
      <Header />
      <RestaurantList />
      <Footer />
    </div>
  );
}
